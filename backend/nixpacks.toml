[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['echo "Build phase - dependencies installed"']

[start]
cmd = 'node migrate-to-single-table.js && sleep infinity'
